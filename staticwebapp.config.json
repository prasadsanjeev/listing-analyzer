{
  "routes": [
    {
      "route": "/api/*",
      "methods": ["get", "post", "options"],
      "allowedRoles": ["anonymous"]
    }
  ],
  "navigationFallback": {
    "rewrite": "/index.html"
  }
}
